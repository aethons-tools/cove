at-task binaries embedded into at-cove for version lockstep (COV-36).

This placeholder keeps `//go:embed bin` compiling in a fresh checkout. The
real per-arch binaries (at-task-linux-amd64, at-task-linux-arm64) are staged
here by scripts/build.sh / the release workflow before at-cove is built, and
are gitignored.
