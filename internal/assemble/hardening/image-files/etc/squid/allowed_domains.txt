# Leading dot = match the domain and all subdomains.
# api.anthropic.com, etc.
.anthropic.com
.claude.com

# claude.ai subscription OAuth login (NOT covered by .claude.com — different TLD).
# Required because managed-settings.json forces forceLoginMethod=claudeai.
claude.ai

# git over HTTPS + raw content + codeload (api./codeload. are *.github.com)
.github.com
.githubusercontent.com

# GitLab (gitlab.com) — git over HTTPS + the v4 API. Self-hosted hosts are added
# per-kit via source-control.gitlab.host (kit-root list), not here.
gitlab.com

# PyPI, if the agent installs Python packages
.pythonhosted.org
pypi.org
